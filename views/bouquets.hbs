<!--Citation for the following code-->
<!--Adapted from Web Application Technology exploration and Step 4 Draft starter code-->
<!--(https://canvas.oregonstate.edu/courses/2031764/pages/exploration-web-application-technology-2?module_item_id=26243419-->
<!--Changes made beyond the starter code are our own without reference to AI-->

<h1>Bouquets</h1>

{{! READ table }}
<table>
    <thead>
    {{! Headers }}
    <tr>
        {{#each bouquets.[0]}}
            <th>
                {{@key}}
            </th>
        {{/each}}
        <th colspan="2">Add New Bouquet</th>    <!-- TODO add anchor to form -->
    </tr>
    </thead>

    <tbody>

    {{! ADD BOUQUET form }}
    <form class='cuForm' id='create_flower_form' METHOD='POST' action='/bouquets/create'>
        <tr>
            <td>Add New</td>
            <td><input type='text' name='bouquet_name' id='bouquet_name' required/></td>
            <td><input type='text' name='bouquet_description' id='bouquet_description'/></td>
            <td><select name='maker_id' id='maker_id' required>
                <option value='NULL' disabled selected>Select a Maker</option>
                {{#each makers}}
                    <option value='{{this.MAKER_ID}}' required>{{this.MAKER_ID}}-{{this.NAME}}</option>
                {{/each}}
            </select></td>
            <td><select name='recipient_id' id='recipient_id'>
                <option value='NULL' disabled selected>Select a Recipient</option>
                {{#each recipients}}
                    <option value='{{this.RECIPIENT_ID}}' required>{{this.RECIPIENT_ID}}-{{this.NAME}}</option>
                {{/each}}
            </select>
            </td>
            <td colspan="2"><input type='submit' value="Add New Bouquet"/></td>
        </tr>
    </form>

    {{! Fields }}
    {{#each bouquets}}
        <tr>
            <td>{{this.NUMBER}}</td>
            <td>{{this.BOUQUET}}</td>
            <td>{{this.DESCRIPTION}}</td>
            <td>{{this.MAKER}}</td>
            <td>{{this.RECIPIENT}}</td>

            {{! UPDATE form }}
            <form id='update_bouquet_form' METHOD='POST' action='/bouquet_edit'>
                <input type='hidden' name='bouquet_id' value='{{this.NUMBER}}'/>
                <input type='hidden' name='bouquet_name' value='{{this.BOUQUET}}' />
                <input type='hidden' name='bouquet_description' value='{{this.DESCRIPTION}}' />
                <input type='hidden' name='maker_id' value='{{this.MAKER}}' />
                <input type='hidden' name='recipient_id' value='{{this.RECIPIENT}}' />
                <td>
                    <button type='submit'>
                        Edit
                    </button>
                </td>
            </form>

            {{! DELETE form }}
            <form id='delete_bouquet_form' METHOD='POST' action='/bouquets/delete'>
                <input type='hidden' name='bouquet_id' value='{{this.NUMBER}}'/>
                <input type='hidden' name='bouquet_name' value='{{this.BOUQUET}}'/>
                <td>
                    <button type='submit'>
                        Delete
                    </button>
                </td>
            </form>
        </tr>
    {{/each}}
    </tbody>
</table>
</br>
